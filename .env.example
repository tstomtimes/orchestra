# Orchestra Plugin Environment Variables
# Copy this file to .env and fill in your actual tokens

# GitHub Integration
# Required for: PR management, repo access, issue tracking
# Scopes needed: repo:status, repo:read, pull_requests:read, pull_requests:write
GITHUB_TOKEN=your_github_token_here

# Vercel Deployment (Optional)
# Required for: Deployment management
# Scopes needed: deployments:read, deployments:write
VERCEL_TOKEN=your_vercel_token_here

# Shopify (Optional)
# Three MCP servers available with different requirements:
#
# 1. Shopify Dev MCP (@shopify/dev-mcp)
#    - Always available via npx (no credentials needed)
#    - Features: API docs, GraphQL validation, Liquid validation
#    - No configuration required
#
# 2. Shopify Theme Server (shopify-server.py)
#    - Requires: SHOPIFY_ADMIN_TOKEN + SHOP_DOMAIN
#    - Features: Theme management, asset CRUD, theme validation
#    - Scopes needed: themes:read, themes:write
#
# 3. Shopify App Server (shopify-app-server.py)
#    - Requires: SHOPIFY_ADMIN_TOKEN + SHOP_DOMAIN
#    - Features: Products, orders, customers, inventory, webhooks, GraphQL queries
#    - Scopes needed: products:read, products:write, orders:read, customers:read, etc.
#
# To enable Theme and App servers, set both variables below:
SHOPIFY_ADMIN_TOKEN=your_shopify_admin_token_here
SHOP_DOMAIN=your-store-name

# Note: SHOP_DOMAIN should be the store name only, not the full URL
# Example: If your store is https://mystore.myshopify.com, use: SHOP_DOMAIN=mystore

# Sanity CMS (Optional)
# Required for: Content management
# Scopes needed: datasets:read, datasets:write
# Note: Also configure PROJECT_ID in mcp.json
SANITY_TOKEN=your_sanity_token_here

# Supabase (Optional)
# Required for: Database and storage operations
# Scopes needed: db:exec, storage:read, storage:write
# Note: Also configure PROJECT_REF in mcp.json
SUPABASE_SERVICE_ROLE=your_supabase_service_role_key_here

# Slack (Optional)
# Required for: Notifications and chat integration
# Scopes needed: chat:write, channels:read
SLACK_BOT_TOKEN=your_slack_bot_token_here

# ElevenLabs Text-to-Speech (Optional)
# Required for: Agent voice notifications
# Get your API key from: https://elevenlabs.io/
# Note: Voice messages are kept short (5-10 words) to minimize token usage
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Voice Settings
# Enable/disable voice notifications for agent task completions
# Set to 'true' to enable voice, 'false' to disable
VOICE_ENABLED=false

# Auto-Commit Settings
# Enable/disable automatic git commits after agent tasks
# Set to 'true' to enable, 'false' to disable
AUTO_COMMIT_ENABLED=true

# Language Settings
# ORCHESTRA_LANGUAGE: Controls language for plugin UI messages, hooks, and agent communications
# COMMIT_LANGUAGE: Controls language for git commit messages only
# Options: 'en' (English) or 'ja' (Japanese)
#
# ORCHESTRA_LANGUAGE affects:
# - Session start messages
# - Agent routing reminders
# - Quality gate messages (PR, merge, deploy hooks)
# - Task clarity reminders
#
# COMMIT_LANGUAGE affects:
# - Git commit message format: prefix: <action> (<reason>)
# - Example (en): feat: Add voice notification feature (to support agent announcements)
# - Example (ja): feat: 音声通知機能を追加 (エージェントのアナウンスをサポートするため)
#
# Note: Code comments always remain in English for international collaboration
ORCHESTRA_LANGUAGE=en
COMMIT_LANGUAGE=en

# Browser Automation
# Playwright-based browser automation for AI-driven testing and web operations
# Installed automatically by setup.sh - allows access to any valid URL
#
# Server port (default: 9222 - Chrome DevTools Protocol port)
BROWSER_MCP_PORT=9222

# Show browser GUI during automation (default: visible for debugging)
# Set to 'true' to run in background/headless mode (useful for CI/production)
BROWSER_HEADLESS=false

# Password-Protected Environment Access
# For testing password-protected sites (Shopify stores, staging environments, etc.)
# Passwords are never logged or exposed - only read from environment variables
#
# Shopify store password (for password-protected stores during development)
SHOPIFY_STORE_PASSWORD=your_store_password_here

# Staging environment password
STAGING_PASSWORD=your_staging_password_here

# Preview environment password
PREVIEW_PASSWORD=your_preview_password_here

# Additional Configuration Notes:
# - Keep this file secure and never commit actual tokens to version control
# - Use least-privilege tokens with minimal required scopes
# - Rotate tokens regularly for security
# - Some services require additional configuration in mcp.json (SHOP_DOMAIN, PROJECT_ID, PROJECT_REF)
